<%= form_with(model: to_see, local: true) do |form| %>
  <div class="field">
    <%= form.label :start_time %>
    <%= form.datetime_select :start_time, id: :to_see_start_time %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

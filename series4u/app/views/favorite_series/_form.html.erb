<%= form_with(model: favorite_series, local: true) do |form| %>
  <% if favorite_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorite_series.errors.count, "error") %> prohibited this favorite_series from being saved:</h2>

      <ul>
      <% favorite_series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
